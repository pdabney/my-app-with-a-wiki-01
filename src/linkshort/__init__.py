"""linkshort — a tiny in-memory URL shortener.

A demonstration app whose every public function carries colocated, machine-checkable
metadata (@intent / @param / @returns / @raises, plus @feature / @flag). A Tier-1
gate verifies that metadata against the code, and a materializer projects it into
this repo's GitHub wiki on every merge to main.
"""
